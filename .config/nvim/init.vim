" Use spaces instead of tabs
set expandtab

" Set the tabstop to 4 characters
set tabstop=4

" Set the number of spaces for one indentation level
set shiftwidth=4

" Make the backspace jump back 4 characters instead of individual spaces
set softtabstop=4

